import 'package:flutter/material.dart';

class SportStatusCard extends StatelessWidget {
  const SportStatusCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Sport Status"));
  }
}
