import 'package:flutter/material.dart';

class PAICard extends StatelessWidget {
  const PAICard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("PAI"));
  }
}
