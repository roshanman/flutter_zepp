import 'package:flutter/material.dart';

class PressureSpo2Card extends StatelessWidget {
  const PressureSpo2Card({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Pressure & Spo2"));
  }
}
