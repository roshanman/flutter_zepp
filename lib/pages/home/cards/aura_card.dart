import 'package:flutter/material.dart';

class AuraCard extends StatelessWidget {
  const AuraCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Aura"));
  }
}
