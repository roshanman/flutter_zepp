import 'package:flutter/material.dart';

class HomeCardSortPage extends StatelessWidget {
  const HomeCardSortPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('卡片排序'),
      ),
      body: const Placeholder(),
    );
  }
}
